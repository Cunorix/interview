import { function as F } from "fp-ts";

// FIXME: Add their return types
export const Create = async () => F.constVoid();

// FIXME: If we can't delete what can we do instead?
export const Delete = async () => F.constVoid();
export const FindAll = async () => F.constVoid();
export const FindById = async () => F.constVoid();

// FIXME: If we can't update what can we do instead?
export const Update = async () => F.constVoid();

export const GetActiveCoupons = async () => F.constVoid();

// TODO: What other useful functions could we add?
